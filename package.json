{
  "name": "findit-campus-platform",
  "private": true,
  "scripts": {
    "dev": "npm --prefix my-app start",
    "build": "npm --prefix my-app run build",
    "test": "npm --prefix my-app test"
  }
}
